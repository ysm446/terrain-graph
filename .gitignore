build/
out/
.vs/
.vscode/
vcpkg_installed/
*.user
*.aps
*.pdb
*.ilk

# ImGui がリポジトリのルートへ吐くレイアウト保存。環境ごとに違うのでコミットしない。
*_imgui.ini

# 手元のデータ置き場。動作確認用の素材、テストプロジェクト、撮ったスクリーンショット。
# テクスチャだけで数百 MB になるうえ、Megascans の素材は再配布できないため、
# まるごとコミットしない。
#
# **コミットするアセットは assets/ へ置く**（同梱するフォントやアイコンなど）。
# 置き場所で扱いが決まるようにして、都度 .gitignore を書き足さなくて済むようにする。
data/
