build/
out/
.vs/
.vscode/
vcpkg_installed/
*.user
*.aps
*.pdb
*.ilk

# ImGui がリポジトリのルートへ吐くレイアウト保存。環境ごとに違うのでコミットしない。
*_imgui.ini

# 動作確認用の素材とテストプロジェクト。テクスチャだけで数百 MB になるうえ、
# Megascans の素材は再配布できないため、まるごとコミットしない。
assets/
